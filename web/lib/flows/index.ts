/* Registers every journey under the id `flow('<id>')` and every data-flow
   reference in the reference file use. Imported once, for its side effects. */
import './contact'
import './nominee'
import './segment'
import './bank'
import './misc'
import './freeze'
import './close'
import './security'
import './rest'
